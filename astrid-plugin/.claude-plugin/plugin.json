{
  "name": "astrid",
  "displayName": "Astrid",
  "version": "0.2.0",
  "description": "Run Claude Code as a governed Astrid agent: capsule tools over MCP, an ephemeral self-cleaning daemon, a SessionStart readiness doctor, an Astrid HUD status line, /astrid:* operator commands, and an Astrid-agent identity. Installing this plugin turns a vanilla `claude` REPL into an Astrid-integrated session.",
  "author": { "name": "Unicity Labs", "email": "info@unicity-labs.com" },
  "homepage": "https://github.com/unicity-astrid/astrid",
  "license": "MIT OR Apache-2.0",
  "keywords": ["astrid", "mcp", "agent", "runtime", "capsule"],
  "mcpServers": "./.mcp.json",
  "hooks": "./hooks/hooks.json",
  "outputStyles": "./output-styles",
  "commands": "./commands",
  "userConfig": {
    "principal": {
      "type": "string",
      "title": "Astrid principal",
      "description": "The Astrid principal this Claude Code session acts as. Scopes tool execution, capability checks, and audit to this identity. Run `astrid agent list` to see available principals. Defaults to 'default'.",
      "required": true,
      "default": "default"
    }
  }
}
